apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "omlorix.fullname" . }}-env
  labels:
    {{- include "omlorix.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
