# Code Highlighting and Previews

Assistant messages can render Markdown tables, task lists, alerts, math, Mermaid diagrams, and Vega or Vega-Lite charts. Fenced code blocks are highlighted according to their language. Use the block's **Code** and **Preview** tabs, **Copy**, or **Download** when available; a preview is a convenience, not proof that the source is safe or correct.

Preview support depends on the content. Examples can include Markdown, Mermaid diagrams, structured data, tables, Vega charts, HTML, and vector graphics. HTML and Vega previews ask before loading external resources; use the option to block them again after inspection.

Python blocks can show **Run Python code** when Code Execution is available. Running it may consume an allowance and can produce files. Inspect the complete block first and never add credentials or sensitive data unless the environment and task are approved for them.

Treat all generated code as untrusted. It can be incomplete, insecure, or incompatible with your environment. The editable or downloaded source can also differ from a sanitized preview, so review it again before executing or publishing it.

For durable editable content, see [Canvas](22_canvas.md). For adjustable explanations, see [Interactive Visualizations](23_visualizations.md).
